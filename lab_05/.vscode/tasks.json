{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make and run C",
            "type": "shell",
            "command": "cd source; make; ./app.exe", // ../bin/app.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}